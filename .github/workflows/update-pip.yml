# This workflow will upload a Python Package to PyPI when a release is created
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python#publishing-to-package-registries

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: version check || Build || update PyPI

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  version-check:
    name: Check release version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install toml requests packaging
      - name: Run version check
        env:
          CHECK_RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: python .github/workflows/version_check.py

  build:
    name: Build distributions
    needs: version-check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build wheel and sdist
        working-directory: ./alpha-mini-rug
        run: |
          python -m build

      - name: Upload distributions as artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-distributions
          path: ./alpha-mini-rug/dist/
          retention-days: 1

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write

    environment:
      name: pypi
      # url: https://pypi.org/project/alpha-mini-rug/${{ github.event.release.name }}
      url: https://pypi.org/project/alpha-mini-rug/${{ github.event.release.tag_name }}

    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          name: python-distributions
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
